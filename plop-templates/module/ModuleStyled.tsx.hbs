import styled from "styled-components"; export const ContainerGeneric = styled.div` display: flex; align-items:
flex-start; justify-content: flex-start; `; export const
{{pascalCase name}}Container = styled(ContainerGeneric)` width: 100%; height: auto; `;